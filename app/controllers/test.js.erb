Mobile.Controllers["Test"] = {
  layout: "BasicLayout",
  needed_components: ["Text"],
};

//Mobile.Controller.beforeFilter({ controllers: 'Test', actions: 'weee'}, function(req) { console.log(req.params); return false; });
