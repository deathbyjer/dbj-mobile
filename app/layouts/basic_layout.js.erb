Mobile.Layout.create("BasicLayout", {
  url: '/mobile/templates/basic_layout.html',
  'class': new Class({
	  Extends: Mobile.Layout,
	  name: "BasicLayout", 
     // default_transition: Mobile.Transitions.Plain,
	  
	  build: function() {
	  },
	  render: function() {
	    this.container.setStyle('width', this.componentParent.container.getSize().x); 
	  }
	})
});