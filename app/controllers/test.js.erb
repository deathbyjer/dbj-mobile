Mobile.Controllers["Test"] = {
  layout: "BasicLayout",
  needed_components: ["Text"],
};

Mobile.Controller.beforeFilter({ controllers: 'Test', actions: 'weee'}, function() { History.replace('/go'); return false; });